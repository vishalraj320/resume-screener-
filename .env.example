# ─── Local development (SQLite — default, no Docker required) ───
DATABASE_URL="file:./dev.db"

# ─── Production (PostgreSQL) ───
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/resume_screener?schema=public"
